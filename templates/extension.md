# Name (expand abbreviations)

One-line summary

## Installation

### Alpine

Note here if not packaged and which versions are affected, or if part of the core `contrib` package

#### Manual

```sh
Make steps
```

#### Packaged

See [installation on Alpine Linux guide](../Guides/alpine.md)

### Settings

```sh
postgresql.conf extract (if required, e.g. shared_preload_libraries)
```

## Usage

- Install extension (requires superuser)
    ```sql
    CREATE EXTENSION xxx; --requires superuser
    ```
- List objects of note
- Provide sample queries

### Sample data